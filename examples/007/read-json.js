const { projectId, startDate, endDate } = require('./data/example-config');

console.log(`Project ID: ${projectId}`);
console.log(`Project start: ${startDate}`);
console.log(`Project end: ${endDate}`);
